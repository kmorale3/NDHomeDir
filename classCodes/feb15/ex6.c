#include <stdio.h>

int main()
{
   int a;
   printf("enter a number: ");
   scanf("%d", &a);
	 if (a>=0)
	 {
      printf("The numberis positive\n");
	   printf("hello\n");
	 }
	 else 
		{
      printf("the number is negative\n");
		printf("whatever\n")
		}

   return 0;

}


// math symbols > >= < <= == !=
// ! not
// && and
// || or
